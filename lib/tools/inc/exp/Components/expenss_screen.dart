import 'package:flutter/material.dart';

class ExpensessReduce extends StatelessWidget {
  const ExpensessReduce({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}